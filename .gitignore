# Classes generated by the build itself
src/main/generated

# Build artifacts in case someone doesn't use a global .gitignore
target

# We encode this for Travis builds, but don't want to check it in to source control!
src/main/resources/build-key.gpg

# Ignore project build tools which are generated on each build;
# we keep Checkstyle and PMD so they can be picked up by Codacy
src/main/tools/travis
src/main/tools/eclipse
